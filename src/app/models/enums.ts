export enum ClientTypes{
    Entity,
    Individual,
}

export enum AccountStatuses{
    Active,
    Blocked,
}

export enum TransactionTypes{
    Incoming,
    Outgoing
}

export enum Months{
    January,
    February,
    March,
    April,
    May,
    June,
    July,
    August,
    September,
    October,
    November,
    December
}
